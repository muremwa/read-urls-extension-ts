{
    "mapFile": "from django.urls import path\n\nfrom . import views\n\napp_name = 'core'\n\n\nurlpatterns = [\n    # accounts/create/ or accounts/edit/ or accounts/get/?username=user\n    path('accounts/<str:action>/', views.account_action, name='account-action'),\n\n    # login/\n    path('login/', views.login, name='login'),\n\n    # profile/create/\n    path('profile/create/', views.profile_create, name='profile-create')]    # profile/delete/1/\n urlspatterns+= [\n    path('profile/delete/<int:profile_pk>/', views.profile_delete, name='profile-delete'),\n]\n",
    "models": [
        "from django.contrib import admin\nfrom django.contrib.admin import register\nfrom .models import Profile, Connection\nfrom notes.models import Note, Comment, Reply\nfrom django.contrib.admin import site\n\n\nadmin.site.register(Profile)\n\n\n@admin.register(Note, Comment, Reply, site=admin.sites.site)\nclass PersonAdmin(admin.ModelAdmin):\n    actions = None\n    list_display = ['user']\n\n\n@admin.register(Connection)\nclass ConnectionAdmin(admin.ModelAdmin):\n    list_filter = ('conn_sender', 'conn_receiver', 'approved')\n    list_display = ('conn_sender', 'conn_receiver', 'approved')\n    actions = ['disapprove_connections', 'approve_connections']\n\n    def disapprove_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=False)\n            self.message_user(request, 'Disapproved selected connections')\n\n    def approve_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=True)\n            self.message_user(request, 'Approved selected connections')\n",
        "from django.contrib.admin import register\nfrom django.contrib import admin\nfrom .models import Profile, Connection\nfrom notes.models import Note, Comment, Reply\nfrom django.contrib.admin import site\n\n# \npan = register\n# admin.site.register(Profile)\n# \n# \n\n@pan(Note, Comment, Reply, site=admin.sites.site)\n# class PersonAdmin(admin.ModelAdmin):\n#     actions = None\n#     list_display = ['user']\n\n\n\n@pan(Connection)\nclass ConnectionAdmin(admin.ModelAdmin):\n    list_filter = ('conn_sender', 'conn_receiver', 'approved')\n    list_display = ('conn_sender', 'conn_receiver', 'approved')\n    actions = ['disapprove_connections', 'approve_connections']\n\n    def disapprove_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=False)\n            self.message_user(request, 'Disapproved selected connections')\n\n    def approve_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=True)\n            self.message_user(request, 'Approved selected connections')\n",
        "# from django.contrib.admin import register\n# from django.contrib import admin\nfrom .models import Profile, Connection\nfrom notes.models import Note, Comment, Reply\nfrom django.contrib.admin import site\n\n#\n# pan = register\n# admin.site.register(Profile)\n#\n#\n\n# @pan(Note, Comment, Reply, site=admin.sites.site)\n# class PersonAdmin(admin.ModelAdmin):\n#     actions = None\n#     list_display = ['user']\n\n\n# @pan(Connection)\nclass ConnectionAdmin(admin.ModelAdmin):\n    list_filter = ('conn_sender', 'conn_receiver', 'approved')\n    list_display = ('conn_sender', 'conn_receiver', 'approved')\n    actions = ['disapprove_connections', 'approve_connections']\n\n    def disapprove_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=False)\n            self.message_user(request, 'Disapproved selected connections')\n\n    def approve_connections(self, request, queryset):\n        if self.has_change_permission(request):\n            queryset.update(approved=True)\n            self.message_user(request, 'Approved selected connections')\n\n\nsite.register(Connection, ConnectionAdmin)\n"
    ],
    "modelsAns": [
        ["Note", "Comment", "Reply", "Profile", "Connection"],
        ["Note", "Comment", "Reply", "Connection"],
        ["Connection"]
    ]
}